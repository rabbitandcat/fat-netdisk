export enum Page {
    bucket,
    transferList,
    transferDone,
    setting,
    services
}


export enum Layout {
    grid,
    table
}

export enum TaskType {
    download,
    upload
}

export enum TransferStatus {
    default,
    done,
    failed
}